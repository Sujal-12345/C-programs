#include <stdio.h>

int main()
{
    float num;
    printf("Enter a number: ");
    scanf("%f",&num);
    if (num%2.0==0)
    {
        printf("Even Number");
    }
    else
    {
        printf("Odd Number");
    }
    return 0;
}
